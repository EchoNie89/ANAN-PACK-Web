---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<p class={`text-xs font-semibold uppercase tracking-[0.18em] text-brand-contrast ${className}`}>
  <slot />
</p>
